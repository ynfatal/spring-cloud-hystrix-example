spring:
  application:
    name: hystrix-consumer-example
  profiles:
    active: default

---
spring:
  profiles: global_configuration

---
spring:
  profiles: specific_configuration

---
spring:
  profiles: default